# Streamlit Cloud Mobile Deployment Guide

## Overview
Deploy your Resume Analyzer to Streamlit Cloud with mobile optimizations for easy access on Android devices.

## Pros
- Easiest deployment method
- No coding required
- Automatic HTTPS
- Free hosting
- Mobile-optimized URL
- Progressive Web App (PWA) support

## Cons
- Requires internet connection
- Limited customization
- Streamlit branding
- Resource limits on free tier

## Step 1: Prepare Mobile-Optimized App

### Create mobile-optimized version
```python
# mobile_app.py
import streamlit as st
import pandas as pd
import plotly.express as px
from pathlib import Path

# Mobile-optimized configuration
st.set_page_config(
    page_title="Resume AI Mobile",
    page_icon="",
    layout="centered",
    initial_sidebar_state="collapsed"
)

# Mobile CSS
MOBILE_CSS = """
<style>
@media (max-width: 768px) {
    .stSelectbox > div > div { font-size: 16px !important; }
    .stButton > button { width: 100% !important; margin: 5px 0; font-size: 16px !important; }
    .stFileUploader { width: 100% !important; }
    .stTextArea textarea { font-size: 16px !important; }
    .stMetric { margin: 5px 0; }
    .stColumns { gap: 10px; }
    .stExpander { margin: 10px 0; }
    footer { visibility: hidden; }
    .mobile-header {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        padding: 20px;
        border-radius: 10px;
        margin: 10px 0;
        text-align: center;
        color: white;
    }
    .mobile-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: #1a1a1a;
        padding: 10px;
        display: flex;
        justify-content: space-around;
        z-index: 999;
    }
    .mobile-nav button {
        background: none;
        border: none;
        color: white;
        padding: 10px;
        border-radius: 5px;
        cursor: pointer;
    }
}
</style>
"""

st.markdown(MOBILE_CSS, unsafe_allow_html=True)

# Mobile-optimized components
def mobile_header(title, subtitle=""):
    st.markdown(f"""
    <div class="mobile-header">
        <h1 style="margin: 0; font-size: 24px;">{title}</h1>
        {f'<p style="margin: 5px 0 0 0; font-size: 14px; opacity: 0.9;">{subtitle}</p>' if subtitle else ''}
    </div>
    """, unsafe_allow_html=True)

def mobile_navigation(current_page="home"):
    nav_items = {
        "home": ("", "Home"),
        "analyzer": ("", "Analyze"),
        "interview": ("", "Interview"),
        "roadmap": ("", "Roadmap")
    }
    
    st.markdown('<div class="mobile-nav">', unsafe_allow_html=True)
    for key, (icon, label) in nav_items.items():
        active = "background: #333;" if key == current_page else ""
        st.markdown(f"""
        <button style="{active}" onclick="window.location.href='?page={key}'">
            {icon}<br>{label}
        </button>
        """, unsafe_allow_html=True)
    st.markdown('</div>', unsafe_allow_html=True)

# Main app
def main():
    # Get page from URL params
    query_params = st.experimental_get_query_params()
    page = query_params.get('page', ['home'])[0]
    
    mobile_header("Resume AI", "Your Career Assistant")
    
    if page == 'home':
        st.markdown("### Welcome to Resume AI")
        st.write("Upload your resume to get instant AI-powered analysis and career guidance.")
        
        uploaded_file = st.file_uploader("Upload Resume", type=['pdf', 'docx'])
        
        if uploaded_file:
            st.success("File uploaded successfully!")
            if st.button("Analyze Resume", type="primary"):
                st.experimental_set_query_params(page='analyzer')
                st.rerun()
    
    elif page == 'analyzer':
        mobile_header("Resume Analysis")
        
        # Simulated analysis
        col1, col2 = st.columns(2)
        with col1:
            st.metric("ATS Score", "85/100")
            st.metric("Skills Match", "78%")
        with col2:
            st.metric("Grade", "B+")
            st.metric("Improvement", "+15%")
        
        st.markdown("### Key Skills")
        skills = ["Python", "JavaScript", "React", "SQL", "Machine Learning"]
        for skill in skills:
            st.write(f"")
        
        st.markdown("### Recommendations")
        recommendations = [
            "Add more technical skills",
            "Include project descriptions",
            "Optimize for keywords"
        ]
        for rec in recommendations:
            st.write(f"- {rec}")
    
    elif page == 'interview':
        mobile_header("Interview Coach")
        
        role = st.selectbox("Target Role", ["Software Engineer", "Web Developer", "Data Analyst"])
        
        if st.button("Start Interview", type="primary"):
            st.info("Interview feature coming soon!")
    
    elif page == 'roadmap':
        mobile_header("Learning Roadmap")
        
        target_role = st.selectbox("Career Goal", ["Software Engineer", "Web Developer", "Data Analyst"])
        time_commitment = st.selectbox("Time Commitment", ["Casual", "Part-time", "Full-time"])
        
        if st.button("Generate Roadmap", type="primary"):
            st.info("Roadmap generation coming soon!")
    
    # Mobile navigation
    mobile_navigation(page)

if __name__ == "__main__":
    main()
```

## Step 2: Create Streamlit Cloud Account

1. **Sign Up**: Go to [share.streamlit.io](https://share.streamlit.io)
2. **Connect GitHub**: Connect your GitHub account
3. **Authorize**: Authorize Streamlit to access your repositories

## Step 3: Prepare Repository

### Create requirements.txt
```txt
streamlit==1.28.0
pandas==2.0.0
plotly==5.15.0
python-docx==0.8.11
pdfplumber==0.9.0
nltk==3.8.1
scikit-learn==1.3.0
Pillow==10.0.0
requests==2.31.0
```

### Create .streamlit/config.toml
```toml
[theme]
primaryColor = "#667eea"
backgroundColor = "#f8f9fa"
secondaryBackgroundColor = "white"
textColor = "#333333"

[server]
headless = true
port = 8501

[browser]
gatherUsageStats = false
```

### Create README.md
```markdown
# Resume AI - Mobile Version

AI-powered resume analysis and career guidance optimized for mobile devices.

## Features
- Resume analysis with ATS scoring
- Interview coaching
- Learning roadmap generation
- Mobile-optimized interface

## How to Use
1. Upload your resume (PDF/DOCX)
2. Get instant AI analysis
3. Practice interviews
4. Generate learning roadmaps

## Mobile Access
- Works on any mobile browser
- Add to home screen for app-like experience
- Progressive Web App (PWA) support
```

## Step 4: Deploy to Streamlit Cloud

### Method 1: GitHub Integration (Recommended)
1. **Push to GitHub**:
   ```bash
   git add .
   git commit -m "Add mobile-optimized Resume Analyzer"
   git push origin main
   ```

2. **Deploy on Streamlit Cloud**:
   - Go to [share.streamlit.io](https://share.streamlit.io)
   - Click "New app"
   - Select your repository
   - Select `mobile_app.py` as main file
   - Click "Deploy"

### Method 2: Direct Upload
1. **Create ZIP file**:
   ```bash
   zip -r resume_ai_mobile.zip . -x ".git/*" "node_modules/*" "__pycache__/*"
   ```

2. **Upload to Streamlit Cloud**:
   - Go to Streamlit Cloud
   - Click "New app"
   - Upload ZIP file
   - Configure deployment

## Step 5: Mobile Optimization

### Add PWA Support
```html
<!-- Add to mobile_app.py -->
st.markdown("""
<link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#667eea">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
""", unsafe_allow_html=True)
```

### Create manifest.json
```json
{
  "name": "Resume AI",
  "short_name": "ResumeAI",
  "description": "AI-powered resume analysis and career guidance",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#667eea",
  "theme_color": "#667eea",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

## Step 6: Test Mobile Experience

### Test on Different Devices
1. **Chrome DevTools**: F12 > Toggle device toolbar
2. **Real devices**: Test on actual phones/tablets
3. **Different browsers**: Chrome, Safari, Firefox

### Mobile Testing Checklist
- [ ] Responsive layout works
- [ ] Touch interactions work
- [ ] File upload works on mobile
- [ ] Navigation is mobile-friendly
- [ ] Performance is acceptable
- [ ] Text is readable without zooming

## Step 7: Add to Home Screen

### Create Installation Instructions
```python
# Add to mobile_app.py
def show_install_prompt():
    if 'install_prompt_shown' not in st.session_state:
        st.markdown("""
        <div style="background: #e3f2fd; padding: 15px; border-radius: 10px; margin: 20px 0;">
            <h4>Install Resume AI on your device</h4>
            <p>For the best experience, add this app to your home screen:</p>
            <ol>
                <li>Tap the share button (Safari: <strong></strong>, Chrome: <strong></strong>)</li>
                <li>Tap "Add to Home Screen"</li>
                <li>Tap "Add" to confirm</li>
            </ol>
        </div>
        """, unsafe_allow_html=True)
        st.session_state.install_prompt_shown = True
```

## Step 8: Performance Optimization

### Optimize for Mobile
```python
# Add caching for better performance
@st.cache_data(ttl=3600)  # Cache for 1 hour
def load_analysis_data():
    # Load heavy data once
    return analysis_data

# Lazy load components
def load_ai_components():
    if 'ai_components' not in st.session_state:
        with st.spinner("Loading AI components..."):
            st.session_state.ai_components = initialize_ai()
    return st.session_state.ai_components
```

### Compress Images
```python
# Optimize images for mobile
from PIL import Image

def optimize_image(image_path, max_size=(800, 600)):
    img = Image.open(image_path)
    img.thumbnail(max_size, Image.Resampling.LANCZOS)
    return img
```

## Step 9: Analytics and Monitoring

### Add Mobile Analytics
```python
# Simple analytics tracking
def track_mobile_usage(page, action):
    analytics_data = {
        'timestamp': datetime.now().isoformat(),
        'page': page,
        'action': action,
        'user_agent': st.experimental_get_query_params().get('ua', ['unknown'])[0]
    }
    
    # Store in session or send to analytics service
    if 'analytics' not in st.session_state:
        st.session_state.analytics = []
    st.session_state.analytics.append(analytics_data)
```

## Step 10: Advanced Features

### Push Notifications (Web Push API)
```javascript
// Add to custom JavaScript
if ('serviceWorker' in navigator && 'PushManager' in window) {
    // Register service worker for push notifications
    navigator.serviceWorker.register('/sw.js')
        .then(registration => {
            // Request notification permission
            return Notification.requestPermission();
        })
        .then(permission => {
            if (permission === 'granted') {
                // Subscribe to push notifications
                subscribeToPushNotifications();
            }
        });
}
```

### Offline Support
```javascript
// Service worker for offline support
self.addEventListener('install', event => {
    event.waitUntil(
        caches.open('resume-ai-v1').then(cache => {
            return cache.addAll([
                '/',
                '/static/css/main.css',
                '/static/js/main.js'
            ]);
        })
    );
});
```

## Step 11: Security Considerations

### Mobile Security
```python
# Add rate limiting
from ratelimit import limits, sleep_and_retry

@sleep_and_retry
@limits(calls=10, period=60)  # 10 calls per minute
def analyze_resume_api(file_data):
    # Your analysis logic
    pass

# Add input validation
def validate_file_upload(file):
    if file.size > 10 * 1024 * 1024:  # 10MB limit
        raise ValueError("File too large")
    
    allowed_types = ['application/pdf', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document']
    if file.type not in allowed_types:
        raise ValueError("Invalid file type")
```

## Step 12: Deployment Checklist

### Pre-Deployment Checklist
- [ ] Mobile-responsive design
- [ ] Performance optimized
- [ ] Error handling implemented
- [ ] Security measures in place
- [ ] Analytics configured
- [ ] PWA manifest created
- [ ] Testing completed
- [ ] Documentation updated

### Post-Deployment Checklist
- [ ] Monitor performance
- [ ] Check mobile analytics
- [ ] Gather user feedback
- [ ] Update based on feedback
- [ ] Regular maintenance

## Benefits of Streamlit Cloud Approach

1. **Zero Coding Required**: Deploy existing app with minimal changes
2. **Free Hosting**: No server costs
3. **Automatic HTTPS**: Secure by default
4. **Mobile URL**: Easy to share and access
5. **PWA Support**: App-like experience on mobile
6. **Easy Updates**: Push updates instantly
7. **Analytics**: Built-in usage tracking

## URL Structure

Once deployed, your app will be available at:
```
https://your-username-resume-ai-mobile.streamlit.app
```

Users can:
- Access directly in mobile browser
- Add to home screen for app experience
- Share link easily
- Use on any device with internet

This Streamlit Cloud approach is the fastest way to get your Resume Analyzer working on Android devices!
